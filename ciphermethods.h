#include "basics.h"

extern char* encryptCaeser(const char* text);
extern char* decryptCaesar(const char* cipher); 
extern char* encryptVigenere(const char* text); 
extern char* decryptVigenere(const char* cipher); 

